# Status bar — uses ~/.config/waybar/ (config.jsonc + style.css)
# Fedora also ships /usr/share/sway/config.d/90-bar.conf with swaybar_command waybar.
# Power button on the bar runs ~/.config/waybar/power-menu.sh → wlogout.

bar {
    swaybar_command waybar
    position top
}
