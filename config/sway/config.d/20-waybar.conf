# Floating waybar (wlr-layer-shell via standalone process, not sway bar { })
# Config: ~/.config/waybar/config.jsonc  style: ~/.config/waybar/style.css
# Disable duplicate bar from /usr/share/sway/config.d/90-bar.conf via empty 90-bar.conf above.

exec_always --no-startup-id waybar
