# Restore last theme on Sway login (~/.local/state/theme-switch/current)
# Runs once per session; reloads colors, wallpaper, and Waybar CSS.

exec --no-startup-id @HOME@/.config/theme-switch/theme-switch.sh login
