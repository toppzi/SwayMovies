# Screenshot shortcuts (overrides /usr/share/sway/config.d/60-bindings-screenshot.conf)
# Saves to ~/Pictures/Screenshots/ and copies to clipboard.

bindsym Print exec $HOME/.config/sway/screenshot-full.sh
bindsym Shift+Print exec $HOME/.config/sway/screenshot-region.sh
bindsym Ctrl+Print exec grimshot save active
